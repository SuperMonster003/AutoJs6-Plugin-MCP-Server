{
  "$name": "English",
  "text_current_lowercase": "current",
  "text_plugin_synopsis": "Exposes device automation to AI agents over the Model Context Protocol",
  "h3_languages_with_ascii": "Languages",
  "p_languages_all_supported_for_readme": "The current README.md supports the following languages",
  "h3_introduction": "Introduction",
  "p_introduction_what": "MCP Server turns an Android device running AutoJs6 into a [Model Context Protocol]({{ mcp_spec_url }}) server. AI agents on a PC, such as Claude Code, Cursor, or the MCP Inspector, connect to the phone over USB or Wi-Fi and use tools to run scripts, read logs, inspect the accessibility node tree, tap and type, take screenshots, and work with files and apps.",
  "p_introduction_how": "The server runs inside the plugin's own process and is reached through a single Streamable HTTP endpoint. AutoJs6 hands the plugin a capability broker over Binder, so every tool call is executed by the host with its existing permissions, engines, and accessibility service; the plugin never duplicates host functionality.",
  "h3_status": "Status",
  "p_status": "The project is in the skeleton stage: this release registers the plugin with the AutoJs6 plugin center and prepares the build, documentation, and test infrastructure. The MCP endpoint and its tools are not available yet. Progress is tracked item by item in [ROADMAP.md]({{ roadmap_url }}).",
  "h3_features": "Planned Features",
  "p_features_intro": "The roadmap delivers the following capabilities in stages",
  "features": [
    "Script execution: run JavaScript from text or from a file inside AutoJs6, list and stop engines, and read recent console output.",
    "Accessibility UI: dump the node tree in a compact text format, find nodes with the AutoJs6 selector syntax, click, long-press, scroll, set text, and press global keys such as Back and Home.",
    "Screenshots: capture the screen as PNG or JPEG with a size cap suitable for multimodal models.",
    "Files, apps, and device: read and write files under the AutoJs6 working directory, launch apps, query the foreground window, and report device information.",
    "Connection paths: USB through `adb forward`, local network with an explicit opt-in, a PC-side stdio bridge, and an optional public tunnel with OAuth 2.1.",
    "Security: a rotating bearer token, first-use pairing confirmation on the phone, and per-group tool switches; the server listens only on the loopback interface by default."
  ],
  "h3_usage": "Usage",
  "usage_steps": [
    "Install the plugin APK from [Releases]({{ repo_url }}/releases) on a device with AutoJs6 build {{ required_host_version_code }} ({{ required_host_version_name }}) or later.",
    "Open the AutoJs6 plugin center, confirm that `MCP Server` is recognized, and enable it. Official release packages pass signature verification automatically.",
    "Turn on the MCP server from the AutoJs6 drawer or the plugin settings page; the phone shows the endpoint address and the pairing token.",
    "On the PC, run `adb forward tcp:{{ mcp_default_port }} tcp:{{ mcp_default_port }}` and point the MCP client at `http://127.0.0.1:{{ mcp_default_port }}{{ mcp_endpoint_path }}` with the token as a bearer credential."
  ],
  "p_usage_note": "Steps 3 and 4 describe the planned workflow and become available once the corresponding roadmap phases are complete. The plugin supports Android {{ min_android_version }} (API {{ android_min_sdk }}) or later.",
  "h3_quick_start": "Client Configuration",
  "p_quick_start_intro": "Claude Code registers the server with one command; other clients use the same URL and header in their MCP configuration",
  "p_quick_start_note": "Replace the token with the value shown on the phone. The command works only after the server can be started (see `Status`).",
  "h3_security": "Permissions and Security",
  "p_security_intro": "The plugin follows explicit boundaries:",
  "security_points": [
    "The Binder entry points are protected by the `org.autojs.permission.PLUGIN` signature permission, so only AutoJs6 can bind to them.",
    "The INTERNET permission is used only for the plugin's own HTTP listener; the plugin makes no outbound requests and collects no data.",
    "Tool calls run through the AutoJs6 capability broker and never exceed what the host itself is allowed to do; dangerous groups such as shell commands and file deletion stay off until the user enables them.",
    "Backups are disabled, and tokens are stored only in the plugin's private storage."
  ],
  "p_security_permission": "Only obtain the plugin from the official [Releases]({{ repo_url }}/releases) page or the AutoJs6 plugin center. Packages from unknown sources may fail host verification or carry risks even when the version number looks identical.",
  "h3_plugin_interface": "Plugin Interface",
  "p_plugin_interface": "The following information targets AutoJs6 host and plugin developers; the host uses these identifiers to discover the plugin and negotiate compatibility",
  "p_contract_service": "`McpServerPluginService` answers the `{{ plugin_service_action }}` action (category `{{ plugin_service_category }}`) and runs in the `:mcp_server` process. The AIDL contract `{{ plugin_aidl_interface }}` is defined by the host in its `mcp-server-api` module and lands with roadmap phase P1; until then the service exposes only the contract descriptor. `McpServerPluginInfoService` answers `{{ plugin_info_action }}` with the standard `PluginInfo`, and `WakeActivity` lets the host wake the plugin process on devices that keep newly installed apps stopped.",
  "h3_roadmap": "Roadmap",
  "p_roadmap": "The plugin's plans and progress are maintained as a checkable list in ROADMAP.md, organized by phase with acceptance criteria and evidence levels. Unchecked items express intent rather than current capabilities; discussion via Issues is welcome.",
  "text_link_roadmap": "View ROADMAP.md",
  "h3_release_history": "Release History",
  "h5_for_more_release_history": "For more release history",
  "h3_build": "Build and Verification",
  "p_build_intro": "This section targets developers who want to build the plugin from source; regular users can simply install the prebuilt APK from the Releases page.",
  "p_build_debug": "Build a debug APK",
  "p_build_test": "Run JVM unit tests and build the instrumentation test APK",
  "p_build_release": "Build the release APK",
  "p_build_digest": "Collect the release artifact and append the version and CRC32 digest to its file name",
  "p_build_docs_check": "Verify that the multilingual documentation sources and generated artifacts are in sync (also enforced by CI)",
  "p_build_requirements": "Building requires JDK 21 or later and Android SDK 36; Gradle and plugin versions are managed centrally by `version.properties` and `io.github.supermonster003.autojs6-platform-versions`.",
  "h3_resource_layout": "Localization and Docs Generation",
  "p_resource_layout": "The language JSON files under `.readme/` and `.changelog/` are the single source for the README, the plugin-center instructions, and the changelog. Always edit those JSON sources and rerun `py .python/generate_markdown.py`; generated README, `plugin_instruction.md`, and changelog artifacts are never edited by hand. Run `py .python/generate_markdown.py --check` to verify all generated artifacts.",
  "h3_license": "License",
  "p_license": "The project code is licensed under the [Mozilla Public License 2.0]({{ license_url }}). Third-party components and their licenses are listed in [Third-Party Notices]({{ third_party_notices_url }}).",
  "h3_links": "Links",
  "text_link_autojs6": "AutoJs6 project",
  "text_link_autojs6_docs": "AutoJs6 documentation",
  "text_link_mcp_spec": "Model Context Protocol specification",
  "text_link_third_party_notices": "Third-party notices",
  "p_instruction_more": "See the [project README]({{ repo_url }}) and [ROADMAP.md]({{ roadmap_url }}) for the connection guide and the current progress."
}
